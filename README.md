# dotfiles

Some of my dotfiles and configuration.

## Setup

- Run `./bootstrap.sh` for a fresh machine (installs Homebrew + just, then runs `just fresh-install`).
- Run `just` to list available tasks.
